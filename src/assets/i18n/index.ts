import * as common from './common';
import * as dashboard from './dashboard';
import * as customer from './customer';
import * as countryCode from './country';
import * as languageCode from './language';

export const enTransLations = {
  ...common.enTranslations,
  ...customer.enTranslations,
  ...dashboard.enTranslations,
  ...countryCode.enTranslations,
  ...languageCode.enTranslations,
};

export const deTransLations = {
  ...common.deTranslations,
  ...customer.deTranslations,
  ...dashboard.deTranslations,
  ...countryCode.deTranslations,
  ...languageCode.deTranslations,
};

export const frTransLations = {
  ...common.frTranslations,
  ...customer.frTranslations,
  ...dashboard.frTranslations,
  ...countryCode.frTranslations,
  ...countryCode.frTranslations,
};

export const itTransLations = {
  ...common.itTranslations,
  ...customer.itTranslations,
  ...dashboard.itTranslations,
  ...countryCode.itTranslations,
  ...countryCode.itTranslations,
};
