import TextWrapper from '@components/text/TextWrapper';

export default function Dashboard() {
  return (
    <div className="flex w-full flex-col gap-5">
      <TextWrapper content={'Dashboard'} />
    </div>
  );
}
